<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="updatePersonInChargeNames">
    <select id="getYycDefPicName" resultType="hmap">
        SELECT * from customers_person_incharge where
            tenant_id = 65 and person_incharge = '-No Sales Employee-'

    </select>
    <delete id="updateYycPicName">
        delete from customers_person_incharge where
            tenant_id = 65 and person_incharge = '-No Sales Employee-'
    </delete>

</mapper>
